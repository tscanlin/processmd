module.exports = {
  // The directory output will be processed to.
  outputDir: './dist',
  // Include the directory as `dir` on the resulting json objects.
  includeDir: true,
  // Include the filename as `base` on the resulting json objects.
  includeBase: true,
  // Include the extension as `ext` on the resulting json objects.
  includeExt: true,
  // Convert mode. Possible options for this are 'json' or 'source'
  convertMode: 'json'
}
