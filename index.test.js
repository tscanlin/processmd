const processto = require('./index');

// _findCommonDir
// _isMarkdown

describe('processto', () => {
  it('should process a directory properly', () => {
    expect((3)).toBe(3);
  });

  // const bar = {
  //   foo: {
  //     x: 1,
  //     y: 2,
  //   },
  // };
  //
  // expect(bar).toMatchSnapshot();
});
